"""AWS account assessments, one KPI per subpackage.

Each subpackage scores accounts on one KPI and returns a report in the same
shape, so several can be rolled up into one organization-wide score. The first
is `assessment.resilience`.
"""
